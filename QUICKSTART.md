# 🎯 Быстрый старт после улучшений

## ✅ Что было улучшено

### Backend (Production-Ready)
- ✅ MongoDB вместо mock данных
- ✅ Полная валидация
- ✅ Rate limiting
- ✅ Улучшенная безопасность
- ✅ Централизованная обработка ошибок
- ✅ JWT с refresh токенами в БД

### Frontend
- ✅ Переменные окружения для API
- ✅ Accessibility улучшения
- ✅ Обновленная документация

## 🚀 Запуск обновленного проекта

### 1. Backend Setup (5 минут)

```bash
# 1. Перейдите в директорию сервера
cd server

# 2. Установите новые зависимости
npm install

# 3. Создайте .env файл (если еще не создан)
# Скопируйте из .env.example и настройте
cp .env.example .env

# 4. Убедитесь что MongoDB запущен
# Windows: net start MongoDB
# Linux/Mac: sudo systemctl start mongod

# 5. Создайте администратора
npm run create-admin

# 6. Запустите новый сервер
node server-new.js
# ИЛИ переименуйте server-new.js в server.js и используйте npm run dev
```

### 2. Frontend Setup (2 минуты)

```bash
# 1. Вернитесь в корневую директорию
cd ..

# 2. Создайте .env файл (если еще не создан)
cp .env.example .env

# 3. Запустите фронтенд
npm run dev
```

### 3. Первый вход

1. Откройте http://localhost:5173
2. Перейдите в админ-панель
3. Войдите с учетными данными:
   - **Username**: `admin`
   - **Password**: `admin123`
4. ⚠️ Смените пароль после входа!

## 📊 Проверка работы

### Health Check
```bash
curl http://localhost:5000/health
```

Ожидаемый ответ:
```json
{
  "status": "OK",
  "timestamp": "2026-02-11T...",
  "environment": "development",
  "database": "connected"
}
```

### Тест авторизации
```bash
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

## 📁 Новые файлы

### Backend
```
server/
├── models/
│   └── User.js                    ✨ Модель пользователя
├── middleware/
│   ├── errorHandler.js            ✨ Обработка ошибок
│   └── rateLimiter.js             ✨ Rate limiting
├── routes/
│   └── auth.js                    🔄 Переписан
├── utils/
│   ├── jwt.js                     ✨ JWT утилиты
│   ├── crypto.js                  ✨ Криптография
│   └── validators.js              ✨ Валидаторы
├── scripts/
│   └── createAdmin.js             ✨ Создание админа
├── server-new.js                  ✨ Новый сервер
├── MIGRATION.md                   ✨ Руководство
└── .env.example                   🔄 Обновлен
```

### Frontend
```
src/
├── vite-env.d.ts                  ✨ Типы для env
├── context/
│   ├── AuthContext.tsx            🔄 Использует env
│   └── DataContext.tsx            🔄 Использует env
└── pages/admin/
    ├── AdminServices.tsx          🔄 Accessibility
    └── MediaManager.tsx           🔄 Accessibility + env
```

### Root
```
├── .env                           ✨ Переменные окружения
├── .env.example                   ✨ Пример
├── CHANGELOG.md                   ✨ История изменений
└── README.md                      🔄 Обновлен
```

## 🔑 Важные изменения

### 1. API URL теперь настраиваемый

**Раньше** (hardcoded):
```typescript
const API_BASE_URL = 'http://localhost:5000';
```

**Теперь** (из .env):
```typescript
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000';
```

### 2. Пользователи теперь в MongoDB

**Раньше**:
```javascript
const users = [
    { id: 1, username: 'admin', ... }
];
```

**Теперь**:
```javascript
const User = require('./models/User');
const user = await User.findOne({ username });
```

### 3. Rate Limiting включен

- **API общий**: 100 запросов / 15 мин
- **Логин**: 5 попыток / 15 мин
- **Регистрация**: 3 попытки / час
- **Сброс пароля**: 3 попытки / час

## 🛠 Команды

### Backend
```bash
cd server

# Разработка
npm run dev

# Production
npm start

# Создать админа
npm run create-admin
```

### Frontend
```bash
# Разработка
npm run dev

# Сборка
npm run build

# Предпросмотр сборки
npm run preview

# Запустить backend
npm run dev:server
```

## 🔧 Конфигурация

### Backend (.env)
```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/accounting_ecosystem
JWT_SECRET=измените-это-в-production  # ⚠️ ВАЖНО!
CORS_ORIGIN=http://localhost:5173
NODE_ENV=development
```

### Frontend (.env)
```env
VITE_API_URL=http://localhost:5000
```

## ⚠️ Важные замечания

### Для production:

1. **Измените JWT_SECRET** на уникальный ключ
2. **Удалите debugToken** из password reset
3. **Настройте CORS_ORIGIN** на ваш домен
4. **Используйте HTTPS**
5. **Делайте резервные копии** MongoDB
6. **Настройте логирование** в файлы
7. **Используйте PM2** или аналог

### Проблемы?

1. **MongoDB не подключается**
   - Проверьте, запущен ли MongoDB: `mongosh`
   - Проверьте MONGODB_URI в .env

2. **Rate limit ошибки при тестировании**
   - Увеличьте лимиты в `server/middleware/rateLimiter.js`
   - Или подождите 15 минут

3. **Токены не работают**
   - Проверьте JWT_SECRET в .env
   - Убедитесь что используете правильный токен
   - Перелогиньтесь

## 📚 Документация

- [Backend README](server/README.md) - полная документация API
- [MIGRATION.md](server/MIGRATION.md) - руководство по миграции
- [CHANGELOG.md](CHANGELOG.md) - список всех изменений

## 🎉 Готово!

Проект теперь готов к production использованию со всеми best practices:
- ✅ Безопасность
- ✅ Валидация
- ✅ Rate limiting
- ✅ Обработка ошибок
- ✅ Документация
- ✅ Масштабируемость

Хорошей разработки! 🚀
