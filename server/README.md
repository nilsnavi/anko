# 🚀 Backend API для "Экосистема учёта"

## Описание
Production-ready RESTful API с полной аутентификацией, валидацией и защитой для управления административной панелью сайта АНО ПБС «Экосистема учёта».

## ⭐ Особенности

- ✅ **MongoDB** интеграция с Mongoose
- ✅ **JWT аутентификация** с refresh токенами
- ✅ **Rate limiting** для защиты от атак
- ✅ **Комплексная валидация** всех входных данных
- ✅ **Безопасность**: Helmet, CORS, bcrypt
- ✅ **Обработка ошибок** с подробным логированием
- ✅ **Password reset** функциональность
- ✅ **Role-based access control** (RBAC)
- ✅ **Graceful shutdown**

## 🛠 Технологии
- Node.js + Express.js
- MongoDB + Mongoose
- JSON Web Tokens (JWT)
- bcryptjs (password hashing)
- express-rate-limit
- helmet (security)
- morgan (logging)
- dotenv (config)

## 📦 Установка и запуск

### 1. Установите зависимости:
```bash
cd server
npm install
```

### 2. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
PORT=5000
MONGODB_URI=mongodb://localhost:27017/accounting_ecosystem
JWT_SECRET=your-very-secure-secret-key-change-this-in-production
CORS_ORIGIN=http://localhost:5173
NODE_ENV=development
```

⚠️ **ВАЖНО**: Измените `JWT_SECRET` на уникальный секретный ключ!

### 3. Запустите MongoDB

```bash
# Windows (если установлен как служба)
net start MongoDB

# Linux/Mac
sudo systemctl start mongod
```

### 4. Создайте администратора

```bash
npm run create-admin
```

Будут созданы учетные данные:
- Username: `admin`
- Email: `admin@ecosystem-pbs.ru`  
- Password: `admin123`

⚠️ Измените пароль после первого входа!

### 5. Запустите сервер

```bash
# Режим разработки (с автоперезагрузкой)
npm run dev

# Production режим
npm start
```

Сервер запустится на `http://localhost:5000`

## 📊 API Endpoints

### Authentication (`/api/auth`)

| Method | Endpoint | Description | Auth | Rate Limit |
|--------|----------|-------------|------|------------|
| POST | `/register` | Регистрация нового пользователя | - | 3/час |
| POST | `/login` | Вход в систему | - | 5/15мин |
| POST | `/refresh` | Обновление access token | - | - |
| POST | `/logout` | Выход из системы | - | - |
| POST | `/reset-password/request` | Запрос сброса пароля | - | 3/час |
| POST | `/reset-password/confirm` | Подтверждение нового пароля | - | - |
| GET | `/me` | Получить профиль текущего пользователя | ✓ | - |

### Content Management (`/api/content`)
- Управление услугами, новостями, командой, FAQ
- Требует аутентификацию

### Media Management (`/api/media`)
- Загрузка и управление медиа-файлами
- Требует аутентификацию

### Служебные

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/health` | Health check сервера |
| GET | `/api` | Информация об API |

## 🔐 Аутентификация

### Получение токенов

```bash
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

Ответ:
```json
{
  "accessToken": "eyJhbGc...",
  "refreshToken": "eyJhbGc...",
  "user": {
    "id": "...",
    "username": "admin",
    "email": "admin@ecosystem-pbs.ru",
    "role": "admin"
  }
}
```

### Использование токена

Добавьте заголовок `Authorization`:

```bash
curl http://localhost:5000/api/auth/me \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

### Обновление токена

```bash
curl -X POST http://localhost:5000/api/auth/refresh \
  -H "Content-Type: application/json" \
  -d '{"refreshToken":"YOUR_REFRESH_TOKEN"}'
```

## 🔒 Безопасность

### Rate Limiting
- **Общий API**: 100 запросов / 15 минут
- **Вход**: 5 попыток / 15 минут
- **Регистрация**: 3 попытки / час
- **Сброс пароля**: 3 попытки / час

### Токены
- **Access Token**: действителен 24 часа
- **Refresh Token**: действителен 7 дней

### Пароли
- Минимум 6 символов
- Хешируются с помощью bcrypt (10 раундов)
- Никогда не возвращаются в API

## 🏗 Структура проекта

```
server/
├── middleware/
│   ├── auth.js              # JWT аутентификация
│   ├── errorHandler.js      # Обработка ошибок
│   └── rateLimiter.js       # Rate limiting
├── models/
│   ├── User.js              # Модель пользователя
│   └── Content.js           # Модель контента
├── routes/
│   ├── auth.js              # Маршруты аутентификации
│   ├── content.js           # Управление контентом
│   └── media.js             # Управление медиа
├── scripts/
│   └── createAdmin.js       # Создание администратора
├── utils/
│   ├── jwt.js               # JWT утилиты
│   ├── crypto.js            # Криптография
│   └── validators.js        # Валидация данных
├── .env.example             # Пример переменных окружения
├── server.js                # Главный файл сервера
├── package.json
└── README.md
```

## 🧪 Тестирование

### Health Check
```bash
curl http://localhost:5000/health
```

### Регистрация
```bash
curl -X POST http://localhost:5000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username":"testuser",
    "email":"test@example.com",
    "password":"test123"
  }'
```

### Логин
```bash
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

## 🐛 Troubleshooting

### MongoDB не подключается
```bash
# Проверьте статус MongoDB
mongosh

# Проверьте MONGODB_URI в .env файле
```

### Ошибка "Invalid or expired token"
- Токен мог истечь (access: 24ч, refresh: 7 дней)
- Используйте `/api/auth/refresh` для обновления
- Или войдите заново через `/api/auth/login`

### Rate limit exceeded
- Слишком много запросов
- Подождите 15 минут или увеличьте лимиты в `middleware/rateLimiter.js`

## 📝 Миграция

Если вы обновляете существующий проект, см. [MIGRATION.md](./MIGRATION.md) для подробных инструкций.

## 🚀 Production Deployment

### Перед деплоем:

1. ✅ Измените все секретные ключи
2. ✅ Установите `NODE_ENV=production`
3. ✅ Настройте CORS для вашего домена
4. ✅ Используйте HTTPS
5. ✅ Настройте резервное копирование MongoDB
6. ✅ Включите логирование в файлы
7. ✅ Используйте process manager (PM2)
8. ✅ Удалите debug коды из password reset

### Пример с PM2:
```bash
npm install -g pm2
pm2 start server.js --name anko-api
pm2 save
pm2 startup
```

## 📚 Дополнительная документация

- [API полная документация](./API.md) (если есть)
- [Руководство по миграции](./MIGRATION.md)
- [Express.js Best Practices](https://expressjs.com/en/advanced/best-practice-security.html)

## 📄 Лицензия

© АНО ПБС «Экосистема учёта»
{
  id: 1,
  username: 'admin',
  email: 'admin@ecosystem-pbs.ru',
  password: 'hashed_password',
  role: 'admin'
}
```

## Безопасность
- Используются JWT токены с сроком действия 24 часа
- Refresh токены с сроком действия 7 дней
- Пароли хэшируются с помощью bcrypt
- Защита CORS
- Helmet для защиты HTTP заголовков

## Разработка
Для разработки используется nodemon для автоматической перезагрузки сервера при изменениях.