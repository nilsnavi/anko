# Руководство по внесению вклада

Спасибо за ваш интерес к проекту "АНО ПБС Экосистема учёта"! Мы приветствуем любой вклад в развитие проекта.

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [С чего начать](#с-чего-начать)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кода](#стандарты-кода)
- [Коммиты](#коммиты)
- [Pull Request](#pull-request)
- [Отчеты об ошибках](#отчеты-об-ошибках)

## Кодекс поведения

Участвуя в этом проекте, вы соглашаетесь следовать нашим правилам:

- Будьте уважительны к другим участникам
- Используйте конструктивную критику
- Фокусируйтесь на том, что лучше для проекта
- Проявляйте эмпатию к другим участникам

## С чего начать

### 1. Форкните репозиторий

```bash
# Клонируйте ваш форк
git clone https://github.com/your-username/anko.git
cd anko

# Добавьте upstream репозиторий
git remote add upstream https://github.com/nilsnavi/anko.git
```

### 2. Создайте ветку

```bash
# Обновите main ветку
git checkout main
git pull upstream main

# Создайте новую ветку для вашей функции
git checkout -b feature/your-feature-name
```

### 3. Настройте окружение

```bash
# Установите зависимости
npm install

# Frontend
cp .env.example .env
# Отредактируйте .env

# Backend
cd server
npm install
cp .env.example .env
# Отредактируйте .env
```

### 4. Проверьте здоровье системы

```bash
# Windows
scripts\health-check.bat

# Linux/MacOS
./scripts/health-check.sh
```

## Процесс разработки

### Типы изменений

- **feat**: Новая функциональность
- **fix**: Исправление ошибки
- **docs**: Изменения в документации
- **style**: Форматирование кода (без изменения логики)
- **refactor**: Рефакторинг кода
- **perf**: Улучшение производительности
- **test**: Добавление тестов
- **chore**: Обновление зависимостей, конфигураций

### Рабочий процесс

1. **Разработка**
   ```bash
   # Запустите dev-сервер
   npm run dev
   
   # В отдельном терминале для backend
   cd server
   npm run dev
   ```

2. **Тестирование**
   - Убедитесь, что все существующие функции работают
   - Добавьте тесты для новой функциональности
   - Проверьте код на ошибки

3. **Проверка перед коммитом**
   ```bash
   # Проверка на ошибки
   npm run build
   
   # Проверка уязвимостей
   npm audit
   ```

## Стандарты кода

### TypeScript/JavaScript

- Используйте TypeScript для новых компонентов
- Следуйте ESLint правилам проекта
- Используйте функциональные компоненты и хуки в React
- Предпочитайте `const` и `let` вместо `var`
- Используйте async/await вместо callbacks

**Пример:**
```typescript
// Хорошо ✓
const fetchData = async () => {
  try {
    const response = await api.getData();
    return response.data;
  } catch (error) {
    console.error('Error fetching data:', error);
    throw error;
  }
};

// Плохо ✗
function fetchData(callback) {
  api.getData(function(err, data) {
    if (err) callback(err);
    callback(null, data);
  });
}
```

### React компоненты

- Используйте функциональные компоненты
- Деструктурируйте props
- Добавляйте TypeScript типы для props
- Используйте описательные имена
- Добавляйте aria-labels для accessibility

**Пример:**
```tsx
// Хорошо ✓
interface ButtonProps {
  onClick: () => void;
  label: string;
  disabled?: boolean;
}

const Button: React.FC<ButtonProps> = ({ onClick, label, disabled = false }) => {
  return (
    <button
      onClick={onClick}
      disabled={disabled}
      aria-label={label}
      className="btn"
    >
      {label}
    </button>
  );
};

export default Button;
```

### CSS/Tailwind

- Используйте Tailwind CSS классы
- Следуйте принципу mobile-first
- Используйте семантические цвета из темы
- Группируйте классы логически

**Пример:**
```tsx
// Хорошо ✓
<div className="flex items-center justify-between p-4 bg-white rounded-lg shadow-sm">
  <h2 className="text-xl font-bold text-slate-900">Title</h2>
  <button className="px-4 py-2 bg-brand-600 text-white rounded-lg hover:bg-brand-700">
    Action
  </button>
</div>
```

### Backend/Node.js

- Используйте async/await
- Обрабатывайте все ошибки
- Валидируйте входные данные
- Используйте middleware для повторяющейся логики
- Добавляйте комментарии для сложной логики

**Пример:**
```javascript
// Хорошо ✓
const createUser = async (req, res, next) => {
  try {
    // Validate input
    const { error } = validateUserInput(req.body);
    if (error) {
      return res.status(400).json({ message: error.details[0].message });
    }
    
    // Create user
    const user = await User.create(req.body);
    
    res.status(201).json({ user });
  } catch (error) {
    next(error); // Pass to error handler middleware
  }
};
```

## Коммиты

### Формат коммитов

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Примеры:**
```bash
feat(auth): add password reset functionality

- Add password reset endpoint
- Send reset email with token
- Add reset password page

Closes #123

fix(api): handle MongoDB connection errors

refactor(components): improve Button component props

docs(readme): update installation instructions

style(admin): fix formatting in AdminServices
```

### Правила коммитов

- Используйте настоящее время ("add feature", не "added feature")
- Используйте повелительное наклонение ("move cursor to...", не "moves cursor to...")
- Первая строка не более 72 символов
- Ссылайтесь на issues и pull requests в footer

## Pull Request

### Чеклист перед отправкой PR

- [ ] Код следует стандартам проекта
- [ ] Все тесты проходят
- [ ] Добавлена/обновлена документация
- [ ] Нет конфликтов с main веткой
- [ ] Коммиты следуют Conventional Commits
- [ ] PR имеет описательное название
- [ ] Добавлено описание изменений

### Отправка PR

1. **Убедитесь, что ваша ветка актуальна:**
   ```bash
   git fetch upstream
   git rebase upstream/main
   ```

2. **Отправьте изменения в ваш форк:**
   ```bash
   git push origin feature/your-feature-name
   ```

3. **Создайте Pull Request:**
   - Перейдите на GitHub
   - Нажмите "New Pull Request"
   - Укажите upstream/main как base и вашу ветку как compare
   - Заполните шаблон PR

### Шаблон Pull Request

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Тестирование
Опишите как вы тестировали изменения

## Чеклист
- [ ] Код следует стандартам проекта
- [ ] Добавлены тесты
- [ ] Обновлена документация
- [ ] Нет breaking changes (или они документированы)

## Связанные issues
Closes #issue_number
```

### Процесс ревью

- Код будет проверен мейнтейнерами
- Может потребоваться внести изменения
- После одобрения PR будет смержен

## Отчеты об ошибках

### Как сообщить об ошибке

1. Проверьте, что ошибка еще не была сообщена
2. Создайте новый issue
3. Используйте шаблон ниже

### Шаблон отчета об ошибке

```markdown
## Описание ошибки
Четкое и краткое описание того, что произошло

## Шаги для воспроизведения
1. Перейдите к '...'
2. Нажмите на '...'
3. Прокрутите вниз до '...'
4. Увидите ошибку

## Ожидаемое поведение
Что должно было произойти

## Скриншоты
Если возможно, добавьте скриншоты

## Окружение
- OS: [e.g., Windows 11, Ubuntu 22.04]
- Browser: [e.g., Chrome 120, Firefox 121]
- Node.js version: [e.g., 18.17.0]
- Project version: [e.g., 1.0.0]

## Дополнительный контекст
Любая другая полезная информация
```

## Предложения функций

### Как предложить новую функцию

1. Проверьте, что функция еще не была предложена
2. Создайте новый issue с меткой "feature request"
3. Опишите функцию детально

### Шаблон предложения функции

```markdown
## Описание функции
Четкое и краткое описание функции

## Проблема, которую решает
Почему эта функция нужна? Какую проблему она решает?

## Предлагаемое решение
Как вы видите реализацию этой функции?

## Альтернативы
Рассматривали ли вы альтернативные решения?

## Дополнительный контекст
Скриншоты, диаграммы, ссылки на похожие функции в других проектах
```

## Вопросы?

Если у вас есть вопросы о процессе внесения вклада, не стесняйтесь:

- Открыть issue с меткой "question"
- Связаться с мейнтейнерами

## Лицензия

Внося вклад в этот проект, вы соглашаетесь с тем, что ваш вклад будет лицензирован под той же лицензией, что и проект.

---

**Спасибо за ваш вклад! 🎉**
